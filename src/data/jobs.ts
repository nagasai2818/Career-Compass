import { Job } from '../types';

/**
 * JOBS is now an empty array by default. 
 * Real job recommendations are generated in real-time by Google Gemini AI 
 * based on the user's resume analysis on the Learning page.
 */
export const JOBS: Job[] = [];
